<script>
  $(document).ready(function () {
    //$("div#[class^='alert']").fadeOut(3000);
  });
</script>
<div class="navbar-inner">
  <div class="container">
    <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </a>
    <div class="nav-collapse">
      <ul class="nav pull-left">
        <li><%= link_to raw("<i class='icon-home'></i> Home"), user_signed_in? ? current_user : root_path %></li>
      <% if user_signed_in? %>
        <li><%= link_to raw("<i class='icon-user'></i> Users"), users_path %></li>
      <% end %>
      <li><%= link_to raw("<i class='icon-flag'></i> Help"), help_path %></li>
      
      </ul>
      <ul class="nav pull-right">
      <% if user_signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class='icon-cog'></i> <%= current_user.email %><b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to raw("<i class='icon-eye-open'></i> View"), current_user %></li>
            <li><%= link_to raw("<i class='icon-pencil'></i> Edit"), edit_user_path(current_user) %></li>
            <li><%= link_to raw("<i class='icon-leaf'></i> VK accounts (#{current_user.accounts.count})"), user_accounts_path(current_user) %></li>
            <li><%= link_to raw("<i class='icon-fire'></i> Bots (#{current_user.bots.count})"), user_bots_path(current_user) %></li>
            <li class="divider"></li>
            <li><%= link_to raw("<i class='icon-off'></i> Sign out"), destroy_user_session_path, :method => :delete %></li>
          </ul>
        </li>
      <% else %>
        <li><%= link_to "Sign in", signin_path %></li>
      <% end %>
      </ul>
    </div>
  </div>
</div>
